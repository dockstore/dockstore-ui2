version: 2
updates:
  # Enable version updates for npm
  - package-ecosystem: "npm"
    # Look for package.json and lock files in the root dir
    directory: "/"
    # Check the npm registry for updates every day
    schedule:
      interval: "daily"
