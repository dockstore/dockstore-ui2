/**
 * Note: The strings are directly displayed to end user ('workflow', 'service')
 * which is why BioWorkflow has the 'workflow' string instead
 *
 * @export
 * @enum {number}
 */
export enum WorkflowClass {
    BioWorkflow = 'workflow',
    Service = 'service'
}
